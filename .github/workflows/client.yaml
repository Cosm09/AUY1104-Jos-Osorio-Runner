on: [push]

jobs:
  mi-job:
    uses: Cosm09/AUY1104-Jos.osorio-Main/.github/workflows/deploy-api.yaml@main
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}